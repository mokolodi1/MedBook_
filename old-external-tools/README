This external-tools folder contains legacy external tools.

Apps should not link to tools in this folder; instead, apps should contain
their own external-tools subfolder with the tools in use, thus allowing
different apps to use their own versions of each tool.
